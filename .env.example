# Steep AI Project Environment Variables
# 
# This project uses service-specific environment files:
#
# Frontend environment variables:
#   → See frontend/.env.example
#   → Used for: Clerk authentication, Next.js config
#
# Backend environment variables:
#   → See backend/.env.example  
#   → Used for: AI API keys, database config, Docker settings
#

# Project-wide shared configurations (if any)
PROJECT_NAME=steep-ai
ENVIRONMENT=development
